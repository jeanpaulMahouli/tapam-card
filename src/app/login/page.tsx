import Link from "next/link";
import { login } from "./actions";
export default async function Login({ searchParams }: { searchParams: Promise<{error?:string}> }) { const { error } = await searchParams; return <main className="mx-auto flex min-h-screen max-w-md items-center p-5"><div className="card w-full p-7"><Link href="/" className="font-black tracking-widest">TAPAM <span className="text-gold">CARD</span></Link><h1 className="mt-8 text-3xl font-black">Connexion</h1><p className="muted mt-2">Accédez à votre carte depuis votre téléphone.</p>{error && <p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">Identifiants invalides.</p>}<form action={login} className="mt-7 space-y-4"><div><label>Identifiant</label><input className="input" name="username" required autoComplete="username"/></div><div><label>Mot de passe</label><input className="input" name="password" type="password" required autoComplete="current-password"/></div><button className="btn btn-primary w-full" type="submit">Se connecter</button></form><Link className="muted mt-5 block text-sm underline" href="/forgot-password">Mot de passe oublié ?</Link></div></main>; }
