import { requireAdmin } from "@/lib/auth";import { prisma } from "@/lib/prisma";export default async function Users(){await requireAdmin();const users=await prisma.user.findMany({where:{role:"USER"},include:{cards:true,profile:true}});return <main className="shell py-8"><h1 className="text-3xl font-black">Utilisateurs</h1><div className="card mt-6 divide-y">{users.map(u=><div className="flex justify-between p-4" key={u.id}><span>{u.profile?.firstName || u.username}</span><span className="muted">{u.cards[0]?.cardNumber || "Sans carte"}</span></div>)}</div></main>}
