import Link from "next/link";import { prisma } from "@/lib/prisma";import { requireAdmin } from "@/lib/auth";
export default async function Admin(){await requireAdmin();const [cards,users,active,suspended,profiles]=await Promise.all([prisma.card.count(),prisma.user.count({where:{role:"USER"}}),prisma.card.count({where:{status:"ACTIVE"}}),prisma.card.count({where:{status:"SUSPENDED"}}),prisma.profile.count({where:{isPublished:false}})]);return <main className="shell py-8"><p className="font-black tracking-widest text-gold">ADMINISTRATION</p><h1 className="mt-2 text-3xl font-black">TAPAM CARD</h1><div className="mt-7 grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{[["Cartes",cards],["Utilisateurs",users],["Actives",active],["Suspendues",suspended],["Non configurées",profiles]].map(([n,v])=><div className="card p-5" key={String(n)}><p className="muted text-sm">{n}</p><p className="mt-2 text-3xl font-black">{v}</p></div>)}</div><div className="mt-7 flex gap-3"><Link href="/admin/cards/new" className="btn btn-primary">Créer une carte</Link><Link href="/admin/cards" className="btn btn-soft">Gérer les cartes</Link></div></main>}
