import {changePassword} from "../actions";import {BackButton} from "@/components/back-button";export default async function Security({searchParams}:{searchParams:Promise<{first?:string,error?:string}>}){const q=await searchParams;const error=q.error==="match"?"Les deux mots de passe sont différents.":q.error?"Utilisez au moins 10 caractères.":null;return <main className="mx-auto max-w-md p-6 pt-16"><BackButton/><div className="card p-7"><p className="eyebrow">Compte sécurisé</p><h1 className="mt-2 text-2xl font-black">{q.first?"Bienvenue sur TAPAM CARD":"Mettre à jour le mot de passe"}</h1><p className="muted mt-3">{q.first?"Choisissez un nouveau mot de passe personnel avant de continuer.":"Choisissez un mot de passe long et unique."}</p>{error&&<p className="mt-4 rounded-xl bg-red-500/10 p-3 text-sm text-red-200">{error}</p>}<form action={changePassword} className="mt-6 space-y-4"><div><label>Nouveau mot de passe</label><input className="input" name="password" type="password" minLength={10} required/><p className="muted mt-2 text-xs">10 caractères minimum.</p></div><div><label>Confirmer le mot de passe</label><input className="input" name="confirmPassword" type="password" minLength={10} required/><p className="muted mt-2 text-xs">Saisissez-le une seconde fois pour éviter toute erreur.</p></div><button className="btn btn-gold mt-2 w-full">Mettre à jour mon mot de passe</button></form></div></main>}
